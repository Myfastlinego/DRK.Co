# Admin Logout

The secure-admin branch now includes the browser-safe Supabase auth helper.

Logout should be wired from the website UI to the Supabase local-session sign-out flow before merging this branch to `main`.
